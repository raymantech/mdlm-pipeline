name: MDLM Scheduler PROBE

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "SCHEDULE OK"
          TZ=Asia/Shanghai date
          date -u
